__pycache__/
.pytest_cache/
.venv/
venv/
.env

# Credential keys: the canonical name and any JSON dropped in the repo root
# (Google key downloads look like ducks-rating-<hex>.json)
service_account.json
/*.json
